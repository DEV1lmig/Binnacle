/**
 * Binnacle Design System v2 - Futuristic HUD Aesthetic
 *
 * Re-exports the canonical tokens from `@binnacle/design-tokens`. Kept as a
 * local entry point so existing imports (`@/app/lib/design-system`) keep
 * working without touching ~25 consumer files.
 */

export * from "@binnacle/design-tokens";
